import { useEffect, useMemo, useState } from 'react';
import { View, Text, Pressable, StyleSheet, ActivityIndicator } from 'react-native';
import { Brand, Fonts, Spacing, ThemeColors } from '@/constants/theme';
import { Recommendation, previewPaperOrder, submitPaperOrder } from '@/lib/api';
import { getDemoMode } from '@/lib/demo-mode';
import { executeDemoTrade, getDemoWallet } from '@/lib/demo-trading';
const CALL_LABEL:Record<string,string>={BUY:'خرید',SELL:'فروش',HOLD:'نگهداری'};const AGENT_LABEL:Record<string,string>={fundamental:'بنیادی',risk:'ریسک',forecast:'پیش‌بینی',comparison:'مقایسه'};function callColor(c:string){return c==='BUY'?Brand.stockGreen:c==='SELL'?Brand.negative:Brand.dataViolet}
type SimState={status:'idle'}|{status:'loading'}|{status:'filled';note:string}|{status:'pending';note:string}|{status:'rejected';reasons:string[]}|{status:'error';message:string};
function FundamentalChart({rec,colors}:{rec:Recommendation;colors:ThemeColors}){const f=rec.codalFundamentals;const vals=useMemo(()=>{if(!f)return[];const rows:[string,number][]=[];if(typeof f.revenue_yoy_pct==='number')rows.push(['رشد درآمد',f.revenue_yoy_pct]);if(typeof f.net_margin_pct==='number')rows.push(['حاشیه سود',f.net_margin_pct]);if(typeof f.net_margin_prev_pct==='number')rows.push(['حاشیه قبلی',f.net_margin_prev_pct]);return rows},[f]);if(!vals.length)return null;const max=Math.max(1,...vals.map(([,v])=>Math.abs(v)));return <View style={[styles.chart,{backgroundColor:colors.backgroundSelected}]}><Text style={[styles.fundTitle,{color:colors.text}]}>نمودار واقعی بنیادی CODAL</Text>{vals.map(([label,v])=><View key={label} style={styles.chartRow}><Text style={[styles.chartValue,{color:v>=0?Brand.stockGreen:Brand.negative}]}>{v.toFixed(1)}٪</Text><View style={[styles.chartTrack,{backgroundColor:colors.backgroundElement}]}><View style={[styles.chartFill,{width:`${Math.max(6,Math.abs(v)/max*100)}%`,backgroundColor:v>=0?Brand.stockGreen:Brand.negative}]} /></View><Text style={[styles.chartLabel,{color:colors.textSecondary}]}>{label}</Text></View>)}</View>}
export function RecommendationCard({rec,colors}:{rec:Recommendation;colors:ThemeColors}){const[sim,setSim]=useState<SimState>({status:'idle'});const[expanded,setExpanded]=useState(false);const[demo,setDemo]=useState(false);const[demoCash,setDemoCash]=useState<number|null>(null);const accent=callColor(rec.call);const directional=rec.call==='BUY'||rec.call==='SELL';const top=[...rec.breakdown].sort((a,b)=>(b.weight_normalized??0)-(a.weight_normalized??0))[0];const sourceLabel=rec.dataSource==='live'?'TSETMC + CODAL':rec.dataSource==='codal'?'CODAL':'نمونه';useEffect(()=>{getDemoMode().then(async e=>{setDemo(e);if(e)setDemoCash((await getDemoWallet()).cash)})},[]);
const runDemoTrade=async(side:'BUY'|'SELL')=>{const price=rec.livePrice?.lastPrice??rec.livePrice?.closingPrice??null;if(!price){setSim({status:'error',message:'قیمت معتبر برای معامله دمو در دسترس نیست.'});return}setSim({status:'loading'});const r=await executeDemoTrade({code:rec.code,side,quantity:10,price});if(!r.ok)setSim({status:'error',message:r.message});else{setDemoCash(r.wallet.cash);setSim({status:'filled',note:`${side==='BUY'?'خرید':'فروش'} دمو ۱۰ سهم ثبت شد.`})}};
const runPaperSim=async()=>{if(!directional)return;setSim({status:'loading'});const preview=await previewPaperOrder({code:rec.code,side:rec.call as 'BUY'|'SELL',quantity:10});if(!preview.ok){if(preview.riskRejected)setSim({status:'rejected',reasons:preview.risk.reasons});else setSim({status:'error',message:preview.message});return}const s=await submitPaperOrder(preview.intent.id);if(!s.ok){setSim({status:'error',message:s.message});return}if(s.receipt.status==='PAPER_FILLED')setSim({status:'filled',note:s.receipt.note??'شبیه‌سازی انجام شد'});else setSim({status:'pending',note:s.receipt.note??'در انتظار تأیید'})};
return <View style={[styles.wrap,{backgroundColor:colors.backgroundElement}]}><View style={styles.header}><View style={[styles.badge,{backgroundColor:`${accent}22`}]}><Text style={[styles.badgeText,{color:accent}]}>{CALL_LABEL[rec.call]??rec.call}</Text></View><View style={styles.titleWrap}><Text style={[styles.title,{color:colors.text}]}>تحلیل کیا‌شا</Text><Text style={[styles.source,{color:colors.textSecondary}]}>منبع: {sourceLabel}</Text></View></View><Text style={[styles.score,{color:colors.textSecondary}]}>امتیاز نهایی: {rec.score>=0?'+':''}{rec.score.toFixed(3)}</Text>{rec.breakdown.length?<View style={styles.weights}><Text style={[styles.weightTitle,{color:colors.text}]}>وزن‌دهی فعال کیا‌شا</Text><View style={styles.weightGrid}>{rec.breakdown.map(b=><View key={b.agent} style={[styles.weightPill,{backgroundColor:colors.backgroundSelected}]}><Text style={[styles.weightName,{color:colors.textSecondary}]}>{AGENT_LABEL[b.agent]??b.agent}</Text><Text style={[styles.weightPct,{color:Brand.primary}]}>{Math.round((b.weight_normalized??0)*100)}٪</Text></View>)}</View></View>:null}{top?<Text style={[styles.reasoning,{color:colors.textSecondary}]} numberOfLines={expanded?undefined:2}>{top.reasoning}</Text>:null}{rec.breakdown.length?<Pressable onPress={()=>setExpanded(e=>!e)}><Text style={[styles.toggle,{color:accent}]}>{expanded?'بستن جزئیات':'جزئیات رأی عامل‌ها'}</Text></Pressable>:null}{expanded?<View style={styles.breakdown}>{rec.breakdown.map(b=><View key={b.agent} style={styles.breakdownRow}><Text style={[styles.breakdownAgent,{color:colors.text}]}>{AGENT_LABEL[b.agent]??b.agent}</Text><Text style={[styles.breakdownDetail,{color:colors.textSecondary}]}>رأی {b.vote>=0?'+':''}{b.vote.toFixed(2)} · اطمینان {(b.confidence*100).toFixed(0)}٪ · وزن {((b.weight_normalized??0)*100).toFixed(0)}٪</Text><Text style={[styles.breakdownDetail,{color:colors.textSecondary}]}>{b.reasoning}</Text></View>)}</View>:null}<FundamentalChart rec={rec} colors={colors}/><View style={styles.simSection}><Text style={[styles.simLabel,{color:colors.textSecondary}]}>{demo?`DEMO WALLET • موجودی ${demoCash===null?'—':Math.round(demoCash).toLocaleString('fa-IR')} ریال`:'Paper — فقط شبیه‌سازی، بدون معامله واقعی'}</Text>{demo?<View style={styles.demoButtons}><Pressable disabled={sim.status==='loading'} onPress={()=>runDemoTrade('BUY')} style={[styles.demoBtn,{backgroundColor:Brand.stockGreen}]}><Text style={styles.simBtnText}>خرید دمو ۱۰ سهم</Text></Pressable><Pressable disabled={sim.status==='loading'} onPress={()=>runDemoTrade('SELL')} style={[styles.demoBtn,{backgroundColor:Brand.negative}]}><Text style={styles.simBtnText}>فروش دمو ۱۰ سهم</Text></Pressable></View>:<Pressable disabled={!directional||sim.status==='loading'} onPress={runPaperSim} style={[styles.simBtn,{backgroundColor:directional?accent:colors.backgroundSelected,opacity:directional?1:.7}]}>{sim.status==='loading'?<ActivityIndicator color="#fff"/>:<Text style={[styles.simBtnText,{color:directional?'#fff':colors.textSecondary}]}>{directional?`شبیه‌سازی ${CALL_LABEL[rec.call]} (۱۰ سهم)`:'بدون سیگنال جهت‌دار'}</Text>}</Pressable>}{sim.status==='filled'?<Text style={[styles.simResult,{color:Brand.stockGreen}]}>✓ {sim.note}</Text>:null}{sim.status==='pending'?<Text style={[styles.simResult,{color:colors.textSecondary}]}>{sim.note}</Text>:null}{sim.status==='rejected'?<Text style={[styles.simResult,{color:Brand.negative}]}>رد شد توسط ریسک: {sim.reasons.join('؛ ')}</Text>:null}{sim.status==='error'?<Text style={[styles.simResult,{color:Brand.negative}]}>{sim.message}</Text>:null}</View></View>}
const styles=StyleSheet.create({wrap:{borderRadius:Spacing.three,padding:Spacing.four,marginTop:Spacing.three,gap:Spacing.two,alignItems:'flex-end'},header:{flexDirection:'row-reverse',alignItems:'center',gap:Spacing.two,width:'100%',justifyContent:'space-between'},titleWrap:{alignItems:'flex-end',flex:1},badge:{paddingHorizontal:Spacing.three,paddingVertical:Spacing.one,borderRadius:Spacing.five},badgeText:{fontFamily:Fonts.sans,fontSize:13,fontWeight:'700'},title:{fontFamily:Fonts.sans,fontSize:15,fontWeight:'800'},source:{fontFamily:Fonts.sans,fontSize:10,marginTop:2},score:{fontFamily:Fonts.mono,fontSize:13},weights:{width:'100%',marginTop:4},weightTitle:{fontFamily:Fonts.sans,fontSize:12,fontWeight:'800',textAlign:'right',marginBottom:6},weightGrid:{flexDirection:'row-reverse',gap:6},weightPill:{flex:1,borderRadius:10,paddingVertical:7,alignItems:'center'},weightName:{fontFamily:Fonts.sans,fontSize:9},weightPct:{fontFamily:Fonts.mono,fontSize:12,fontWeight:'900',marginTop:2},reasoning:{fontFamily:Fonts.sans,fontSize:13,textAlign:'right',lineHeight:20},toggle:{fontFamily:Fonts.sans,fontSize:12,fontWeight:'700'},breakdown:{width:'100%',gap:Spacing.two},breakdownRow:{alignItems:'flex-end'},breakdownAgent:{fontFamily:Fonts.sans,fontSize:12,fontWeight:'700'},breakdownDetail:{fontFamily:Fonts.sans,fontSize:11,textAlign:'right',lineHeight:17},chart:{width:'100%',borderRadius:12,padding:10,marginTop:4},fundTitle:{fontFamily:Fonts.sans,fontSize:12,fontWeight:'800',textAlign:'right',marginBottom:7},chartRow:{flexDirection:'row',alignItems:'center',gap:7,marginVertical:4},chartValue:{width:48,fontFamily:Fonts.mono,fontSize:10},chartTrack:{flex:1,height:7,borderRadius:7,overflow:'hidden'},chartFill:{height:7,borderRadius:7},chartLabel:{width:70,fontFamily:Fonts.sans,fontSize:9,textAlign:'right'},simSection:{width:'100%',marginTop:Spacing.two,gap:Spacing.two,alignItems:'flex-end'},simLabel:{fontFamily:Fonts.sans,fontSize:11},simBtn:{width:'100%',paddingVertical:Spacing.three,borderRadius:Spacing.two,alignItems:'center'},demoButtons:{width:'100%',flexDirection:'row-reverse',gap:Spacing.two},demoBtn:{flex:1,paddingVertical:Spacing.three,borderRadius:Spacing.two,alignItems:'center'},simBtnText:{fontFamily:Fonts.sans,fontSize:13,fontWeight:'700',color:'#fff'},simResult:{fontFamily:Fonts.sans,fontSize:12,textAlign:'right',lineHeight:18}});